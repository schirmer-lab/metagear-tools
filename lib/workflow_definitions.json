{
  "metagear": {
    "workflows": {
      "global_parameters": [
        {
          "name": "outdir",
          "required": false,
          "default": "results",
          "description": "Output directory"
        },
        {
          "name": "help",
          "required": false,
          "description": "Display help information"
        },
        {
          "name": "debug",
          "required": false,
          "default": false,
          "description": "Enable debug mode"
        },
        {
          "name": "config",
          "required": false,
          "description": "Custom configuration file"
        }
      ],
      "definitions": [
        {
          "name": "download_databases",
          "description": "Install Databases (Kneaddata, MetaPhlAn, HUMAnN, CheckM2, MMseqs2 taxonomy, PHOLD)",
          "parameters": [
            {
              "name": "databases",
              "required": false,
              "default": "all",
              "description": "Databases to install. Accepted values: all | genes | virus | contig_classification | mmseqs_taxonomy | structures. Multiple values comma-separated. Default: all. The `contig_classification` group bundles CheckM2 + the MMseqs2 GTDB taxonomy DB; use `mmseqs_taxonomy` to pull only the latter. The `structures` group installs the PHOLD structural DB (~7.7 GB) used by the structures workflow."
            }
          ]
        },
        {
          "name": "qc_dna",
          "description": "Quality Control for DNA",
          "parameters": [
            {
              "name": "input",
              "required": true,
              "description": "Input .csv file for quality control"
            }
          ]
        },
        {
          "name": "qc_rna",
          "description": "Quality Control for RNA",
          "parameters": [
            {
              "name": "input",
              "required": true,
              "description": "Input .csv file for quality control"
            }
          ]
        },
        {
          "name": "microbial_profiles",
          "description": "Get microbial profiles with MetaPhlAn and HUMAnN",
          "parameters": [
            {
              "name": "input",
              "required": true,
              "description": "Input .csv file for microbial profiles"
            },
            {
              "name": "metaphlan_dir",
              "required": false,
              "description": "MetaPhlan profiles (DIRECTORY: optional). Folder containing individual MetaPhlan profiles (.txt)"
            }
          ]
        },
        {
          "name": "genes",
          "description": "Gene centric analysis workflow",
          "parameters": [
            {
              "name": "input",
              "required": true,
              "description": "Input .csv file for gene analysis"
            },
            {
              "name": "contigs_dir",
              "required": false,
              "description": "Contigs (DIRECTORY: optional). Folder containing SAMPLE_ID.contigs.fa.gz files"
            },
            {
              "name": "genes_dir",
              "required": false,
              "description": "Genes (DIRECTORY: optional). Folder containing SAMPLE_ID.all.genes.filtered.fasta files"
            },
            {
              "name": "metaphlan_profiles",
              "required": false,
              "description": "MetaPhlAn profiles (if available)"
            },
            {
              "name": "representative_genes",
              "required": false,
              "description": "Representative-genes catalog (FILE: optional). Dereplicated genes from all samples."
            },
            {
              "name": "representative_proteins",
              "required": false,
              "description": "Representative-proteins catalog (FILE: optional). Dereplicated proteins from all samples."
            },
            {
              "name": "representative_proteins_annotations",
              "required": false,
              "description": "Representative-proteins annotations (FILE: optional). Functional annotations for representative proteins."
            },
            {
              "name": "representative_genes_tpm",
              "required": false,
              "description": "Representative-genes TPM (FILE: optional). TPM values for representative genes."
            },
            {
              "name": "representative_genes_rpkm",
              "required": false,
              "description": "Representative-genes RPKM (FILE: optional). RPKM values for representative genes."
            },
            {
              "name": "representative_genes_count",
              "required": false,
              "description": "Representative-genes counts (FILE: optional). Counts for representative genes."
            }
          ]
        },
        {
          "name": "virus",
          "description": "Viral analysis workflow",
          "parameters": [
            {
              "name": "input",
              "required": true,
              "description": "Input .csv file for viral analysis"
            },
            {
              "name": "contigs_dir",
              "required": false,
              "description": "Contigs (DIRECTORY: optional). Folder containing SAMPLE_ID.contigs.fa.gz files"
            },
            {
              "name": "genes_dir",
              "required": false,
              "description": "Genes (DIRECTORY: optional). Folder containing SAMPLE_ID.all.genes.filtered.fasta files"
            },
            {
              "name": "all_genes",
              "required": false,
              "description": "All-genes catalog (FILE: optional). Concatenated genes from all samples."
            },
            {
              "name": "representative_genes",
              "required": false,
              "description": "Representative-genes catalog (FILE: optional). Dereplicated genes from all samples."
            },
            {
              "name": "representative_proteins",
              "required": false,
              "description": "Representative-proteins catalog (FILE: optional). Dereplicated proteins from all samples."
            },
            {
              "name": "representative_proteins_annotations",
              "required": false,
              "description": "Representative-proteins annotations (FILE: optional). Functional annotations for representative proteins."
            },
            {
              "name": "viral_annotation",
              "required": false,
              "description": "Perform viral annotation (default: true)."
            }
          ]
        },
        {
          "name": "classification",
          "description": "Integrated contig + MAG classification: assembly, viral/plasmid partition via geNomad, bacterial binning (SemiBin2 + MetaBAT2 + Binette + CheckM2), and GTDB-Tk taxonomy on MIMAG-passing bins. v1 minimum; contig fallback, plasmid typing, and per-contig TSV merge are deferred.",
          "parameters": [
            {
              "name": "input",
              "required": true,
              "description": "Input .csv file (samplesheet). Supports an optional `biome` column (between `sample` and `fastq_1`) used to pick the SemiBin2 pretrained model; defaults to `global` when empty."
            },
            {
              "name": "contigs_dir",
              "required": false,
              "description": "Contigs (DIRECTORY: optional). Folder containing SAMPLE_ID.contigs.fa.gz files; skips reassembly when provided."
            },
            {
              "name": "chromosome_dir",
              "required": false,
              "description": "Chromosome partition (DIRECTORY: optional). Folder containing SAMPLE_ID.chromosome.fna.gz files produced by a prior GENOMAD_EXTRACT_CHROMOSOME step. When provided, skips the entire VIRAL_DETECTION subworkflow (geNomad + CheckV passes + chromosome extraction). WARNING: does not check geNomad parameter consistency — re-run VIRAL_DETECTION instead of reusing if FDR thresholds or score calibration have changed."
            },
            {
              "name": "enable_contig_taxonomy",
              "required": false,
              "description": "Run MMseqs2 easy-taxonomy on the unbinned chromosome partition to populate per-contig lineage in the merged TSV. Default false — disabled because per-contig LCA on unbinned contigs is low-confidence and the GTDB DB makes it expensive (requires task.memory >=96 GiB, hours per sample). Bin-attributable contigs already get trustworthy lineage from GTDB-Tk in mag; viral/plasmid contigs from geNomad. Enable only when you want exploratory sub-domain signal on the unbinned set."
            },
            {
              "name": "mmseqs_taxonomy_db",
              "required": false,
              "description": "Path to a pre-built MMseqs2 taxonomy database (e.g. GTDB built via `mmseqs databases GTDB <out> <tmp>`). Used only when --enable_contig_taxonomy is true. Typically set globally in ~/.metagear/metagear.config instead of per-invocation."
            },
            {
              "name": "viral_ids_dir",
              "required": false,
              "description": "Viral contig IDs directory (DIRECTORY: optional). Folder containing SAMPLE_ID_viral_ids_to_keep.txt files produced by a prior virus run (MERGE_TABLES output). Lets the per-contig TSV classify virus contigs even when --chromosome_dir bypassed VIRAL_DETECTION. Auto-discovered by --reuse-outputs."
            },
            {
              "name": "plasmid_ids_dir",
              "required": false,
              "description": "Plasmid contig IDs directory (DIRECTORY: optional). Folder containing SAMPLE_ID_plasmid_ids_to_keep.txt files. Same pattern as --viral_ids_dir."
            },
            {
              "name": "gene_clusters_tsv",
              "required": false,
              "description": "Path to all.genes.clusters.tsv (from a prior genes run). When provided, classification cross-walks per-contig primary_class onto the gene catalog and emits clusters/all.genes.clusters.aggregated.refined.tsv with a multi_class flag for clusters whose members span multiple classes (e.g. shared between bacteria and virus — potentially HGT, AMGs, or other interesting biology). Clean re-derivation from per-contig signal; does NOT merge with virus's existing aggregated TSV. Auto-discovered by --reuse-outputs."
            }
          ]
        },
        {
          "name": "msp",
          "description": "MSP — MetaSpecies Pangenomes. MSPminer co-abundance clustering on the gene catalog, GTDB-Tk taxonomy on MSP representative sequences, and MetaPhlAn cross-walk. Builds on a prior `genes` run; reads the gene catalog and per-sample abundance matrices from disk.",
          "parameters": [
            {
              "name": "input",
              "required": true,
              "description": "Input .csv file (samplesheet). Same shape as genes — needed so MetaPhlAn profiles can be computed when --metaphlan_profiles is not provided."
            },
            {
              "name": "representative_genes",
              "required": false,
              "description": "Representative-genes catalog (FILE). Dereplicated genes from all samples. Required input — auto-discovered by --reuse-outputs from a prior `genes` run; pipeline-side INIT exits with a clear error if neither --reuse-outputs nor an explicit value supplies it."
            },
            {
              "name": "representative_genes_count",
              "required": false,
              "description": "Representative-genes counts (FILE). Required input — auto-discovered by --reuse-outputs."
            },
            {
              "name": "representative_genes_rpkm",
              "required": false,
              "description": "Representative-genes RPKM (FILE). Required input — auto-discovered by --reuse-outputs."
            },
            {
              "name": "metaphlan_profiles",
              "required": false,
              "description": "MetaPhlAn profiles (if available). When omitted, profiles are computed from the samplesheet reads."
            },
            {
              "name": "gtdb_tk_db",
              "required": false,
              "description": "Path to a GTDB-Tk database (R220 or compatible). Typically set globally in ~/.metagear/metagear.config."
            }
          ]
        },
        {
          "name": "mag",
          "description": "MAG mag and taxonomy assignment: dRep (skani backend) on per-sample Binette bins, GTDB-Tk on cluster representatives, coverm-genome MAG×sample abundance, and back-fill of classification's per-contig TSV with cohort lineage. Builds on a prior `classification` run; reads its bin and per-contig outputs from disk.",
          "parameters": [
            {
              "name": "input",
              "required": true,
              "description": "Input .csv file (samplesheet). Same shape as classification — the samplesheet is needed because abundance back-mapping uses the per-sample reads."
            },
            {
              "name": "bins_dir",
              "required": false,
              "description": "Per-sample Binette deliverables (DIRECTORY). Points at classification's `assemblies/bins/` parent directory. Each sample subdir contains `<sample>_binN.fa` (MAG FASTAs, --prefix-ed by Binette at source), `quality_report.tsv` (renamed from final_bins_quality_reports.tsv at publish time), and `contig_to_bin.tsv` (renamed from final_contig_to_bin.tsv). Auto-discovered by --reuse-outputs. The pipeline-side INIT still enforces this (exits with a clear error) if neither --reuse-outputs nor an explicit value supplies it."
            },
            {
              "name": "gtdb_tk_db",
              "required": false,
              "description": "Path to a GTDB-Tk database (R220 or compatible). Typically set globally in ~/.metagear/metagear.config; this parameter exists so a user can point at an alternate release per-invocation."
            }
          ]
        },
        {
          "name": "structures",
          "description": "Protein structural-homology annotation. v1 backend is PHOLD (ProstT5 → 3Di → Foldseek vs the PHOLD curated DB). Builds on a prior virus/genes run; reads the cohort protein catalog, Pfam table, protein clusters, and viral catalog from disk. Emits per-rep PHOLD annotations for the full catalog and the viral catalog separately. Viral coverage is guaranteed in every scope mode via a top-up step.",
          "parameters": [
            {
              "name": "input",
              "required": true,
              "description": "Input .csv file (samplesheet). Used for provenance only — no per-sample reads are processed."
            },
            {
              "name": "representative_proteins",
              "required": false,
              "description": "Full protein catalog (FILE). Auto-discovered by --reuse-outputs from a prior virus/genes run."
            },
            {
              "name": "representative_proteins_clusters",
              "required": false,
              "description": "Protein cluster TSV (FILE). Auto-discovered by --reuse-outputs."
            },
            {
              "name": "viral_representative_proteins",
              "required": false,
              "description": "Viral protein catalog (FILE). Auto-discovered by --reuse-outputs."
            },
            {
              "name": "representative_proteins_annotations",
              "required": false,
              "description": "Pfam / InterProScan annotations TSV (FILE). Optional — when missing the scope filter degrades gracefully (every all-protein-rep is treated as unannotated). Auto-discovered by --reuse-outputs."
            },
            {
              "name": "phold_db",
              "required": false,
              "description": "Path to the PHOLD structural DB directory. Install via `metagear download_databases --databases structures`. Typically set globally in ~/.metagear/metagear.config."
            },
            {
              "name": "structures_scope",
              "required": false,
              "default": "unannotated_plus_duf",
              "description": "Which proteins to annotate. Accepted values: all | unannotated | unannotated_plus_duf | viral_only. Default unannotated_plus_duf — best ROI on annotation gain per compute spent. Viral coverage is guaranteed in every mode except `viral_only` (a top-up step adds viral-gene-rep translations not already in the chosen subset). Use `viral_only` for quick end-to-end validation; use `all` for the full catalog (most expensive)."
            },
            {
              "name": "structures_use_cpu",
              "required": false,
              "default": "true",
              "description": "CPU vs GPU for the ProstT5 prediction step (PHOLD_PREDICT). Default `true` (CPU) so the workflow runs on any node; set to `false` on a GPU-enabled compute node for a 10-50x speedup."
            },
            {
              "name": "structures_batch_size",
              "required": false,
              "default": "1",
              "description": "ProstT5 batch size for PHOLD_PREDICT. Higher = faster on GPU + higher VRAM use. 1 is the PHOLD default and is fine for CPU."
            },
            {
              "name": "structures_predict_shard_size",
              "required": false,
              "default": "5000",
              "description": "Number of proteins per shard for the parallel ProstT5 prediction. ProstT5 model load (~30-60 s per shard) dominates the wall time at small shard sizes, so larger shards amortize that cost. Default 5000 is the sweet spot for cohorts up to ~1M proteins; bump to 10000 for 500-sample-scale production cohorts. Tune down only if a specific GPU can't fit the working set."
            },
            {
              "name": "structures_prepare_for_gpu",
              "required": false,
              "default": "false",
              "description": "Offsite-GPU mode. When `true`, the workflow runs FILTER + SEQKIT_SPLIT2 + PACKAGE_PHOLD_OFFSITE and stops — producing a self-contained rsync-friendly bundle under ${outdir}/structures/offsite_predict/. The user runs the bundled script on a GPU server (no Nextflow / no containers needed), rsyncs the results back into ${outdir}/structures/phold_predict/, then re-invokes `metagear structures` with the same args minus this flag and passing `--phold_predict_dir <outdir>/structures/phold_predict` to resume from MERGE_PHOLD_PREDICTIONS onward."
            },
            {
              "name": "phold_predict_dir",
              "required": false,
              "description": "Path to pre-existing per-shard PHOLD_PREDICT outputs (predict_*/ subdirectories). When set, skips the PHOLD_PREDICT step entirely and consumes the on-disk dirs. Typically set after rsyncing offsite-GPU predict results back into ${outdir}/structures/phold_predict/."
            }
          ]
        }
      ]
    }
  }
}
